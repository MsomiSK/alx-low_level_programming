#include "main.h"
#include <stdio.h>

/**
 * mul - which multiples two integers
 * @a : first interger
 * @b : second integer
 * Return: 0
 */

int mul(int a, int b)
{
	int result = a * b;
return (result);
}
